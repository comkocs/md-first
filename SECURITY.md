# 安全说明

## 先说清楚这东西的信任模型

工单台（`ticket-desk/`）是为**一个人 + 他自己的几扇 AI 窗口**做的。
它的默认假设是：**凡是拿到令牌的，都是你自己。**

下面这些是**有意为之的设计取舍**，不是待修的缺陷。你要把它放到别的环境里用，
得先知道它们：

| 设计 | 后果 | 为什么这么做 |
|---|---|---|
| 令牌是**全权凭据**，没有按人分权 | 拿到令牌就能读写全部工单 | 单人使用，分权只会增加你自己的维护量 |
| 令牌可以走 URL 查询串（`?t=…`） | 可能进浏览器历史、代理日志、访问日志、截图、Referer | 为了让 AI 窗口能一句话打开页面。**你自己那台机器上无所谓，公网上有所谓** |
| 请求里的 `by` 字段**不与登录身份强绑定** | 持有令牌者可以声明任意业务角色 | 角色是记账用的，不是权限边界。单人场景下没有人需要冒充自己 |
| 服务端异常信息会回给客户端 | 可能带出绝对路径、文件名、内部状态 | 自己调试时看得见错误比藏起来有用 |
| `install.sh` 监听 `0.0.0.0` 并放行防火墙端口 | 装完即对所在网络可达 | 为了多机协作能开箱即用 |

## 所以：别把它直接暴露在公网

推荐的用法，按优先级：

1. **单机模式**（默认）——什么都不配，数据落在你自己的用户目录，不开端口。
2. 要多机协作，**放在内网或 VPN 后面**。
3. 非要过公网，**前面挡一层**：反向代理 + 你自己的鉴权，或者至少
   把防火墙规则收窄到固定来源 IP，不要用 `0.0.0.0/0`。

★不要把令牌写进任何会被记录的地方（聊天记录、任务书、截图、CI 变量日志）。
令牌只显示一次，泄漏了就重新生成——旧的会立刻失效。

## 自己部署时的几条

- `ticket_desk/config.json`、`remote.env`、`*.token` 都在 `.gitignore` 里，**别提交**。
- 工单真数据（`tickets/`）也不进仓：它每分钟都在变，而且是你的真实项目内容。
- HTTPS 用你自己的证书；`install.sh` 里那套是给你起步用的，不是给生产用的。

## 报告安全问题

发现了上面没写到的问题，**不要开公开 Issue**——直接发邮件：

**Email:970457@qq.com**

请带上：怎么复现、影响范围、你用的版本（`git rev-parse --short HEAD`）。

这是一个业余时间维护的开源项目，没有 SLA。我会尽量回，但不承诺时限。
属于上面「信任模型」那张表里的已知取舍，我会直接回你一句「这是设计如此」，
并考虑是否要在文档里写得更明白。

## 不在范围内

- 把工单台裸奔在公网上被人读走数据——见上面那张表，这是已知且有意的。
- 你自己的令牌泄漏导致的问题。
- 依赖项的问题（本项目只用 Python 标准库，图片功能可选 Pillow）。
