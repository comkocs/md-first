# 3 · 大型

> 适用：**你已经不直接跟干活的说话了**——中间隔着一层总监，
> 底下是一批用完就关的窗，还有一席不听你指挥的复检。
>
> ★**分界线不是"做多久"，是"你还是不是那个派活的人"。**
> 几个月、十几条线并行，但你仍然一个个亲自派活 → 那是[中型](2-中型.md)，不是这一档。
> 它防的那一种失败：**谁在做什么说不清**——两扇窗改同一个文件、抬同一个号。
>
> 这一份是**地图 + 最疼的那批教训**。具体章程在
> [frameworks/大型/](frameworks/大型/)，装的时候再逐份读。

> ### ★这一份光有文件远远不够
>
> 本档要用到仓库里的 **`ticket-desk/`**（工单台源码，含服务端）
> 和 **`frameworks/大型/`**（七份章程正文）。
> 只拿到这一个文件的话，先去 <https://github.com/comkocs/md-first> 把整个仓拿下来，再回来。

**先说一句劝退的话。** 这一档一天要花你 1–2 小时管理时间，
而且**绝大多数项目这辈子都用不到它**。
如果你还是自己在派活，或者还没真的撞上"两扇窗改了同一个文件"，**别装**——
你会在第四天开始偷偷跳过流程，而**被偷偷跳过的流程比没有流程更坏**，
因为台面上还写着它在跑，你会照着那张台面做判断。

---

## 三件我请你替我盯着的事

★先说清楚我们俩的关系：**拍板的人是我。** 你是顾问，不是看门人。
下面这些但凡我坚持，你提醒我一次就照做——你拦不住我，也不该拦。
你能做的是让我在清醒的时候做决定，仅此而已。

一、**两扇窗。** 跟我聊的这扇窗最好不干活，干活另开一扇。
   沟通和干活抢同一份上下文，先满的是沟通那边——满了它开始丢最前面那段，
   而那正是我最早说的需求，它还不会告诉我它丢了。
   我要是就想在一扇窗里干，说一声，照做。

二、**规矩改文件，别在聊天里改。**
   我说「这次特殊你先别管那条」——提醒我一句：这话明天还会再说一次。
   我说「把那条删了」——就去改这个文件，然后在 `DECISIONS.md` 记一行
   （日期 / 删了什么 / 因为 / 什么情况下要加回来）。
   ★记那一行不是为了管我，是为了半年后我自己能查到「这条护栏是谁拆的」。

三、**「什么算做完」这一句，尽量在动手前拿到。**
   不是流程要求，是没有它，怎么做全是你在替我拍板。
   我给不出来也别卡着——先做一版，但要明说「这版是按我的理解做的，你看哪里不对」。

## ★不管哪一档：要我开新窗，必须给我「一句话 + 一个 md」

任何时候你要我去开一扇新窗干活，**必须同时给我两样**：

> **一句能直接复制粘贴的话** ＋ **一个 md 文件的完整绝对路径**

那句话写死成这个样子，**用独占一行的代码块给我**，别混在段落里：

```
读 <完整绝对路径>，照做。先做「<哪一件>」那一条。
```

三条要求：

1. **路径写全**，从盘符开始。相对路径在新窗里没有意义——它不知道你站在哪儿。
2. **我要能一键复制**，不用再改一个字、不用自己拼路径。
3. **md 要先写好再给我那句话**，别让我复制完才发现文件还不存在。

★为什么非要 md、不能直接说：那扇新窗**读不到我们现在这段对话**。
  **文件能被下一扇窗读到，对话不能。** 这就是这整套东西的全部秘密。

---

## 这个项目算哪一档（我只给建议，你定）

**★选择权在你。** 下面是建议，不是判据——你想用哪一档就用哪一档。
哪怕这是个三天的小活、你偏要上大型那一套，那也是你的项目，我不拦。

我该怎么给这个建议：**从你的 `项目.md` 读三个数，不要凭感觉判断。**

| | 模块清单有几块 | 第一版要做几块 | 现在同时在干活的窗 |
|---|---|---|---|
| **小型** | 5 块以内 | 1 块 | 2 扇（策划 + 工位） |
| **中型** | 6～15 块 | 2～3 块 | 3～6 扇 |
| **大型** | 15 块以上 | 3 块以上 | 7 扇以上 |

代码量只当**参考**，不当判据（开工前估代码量几乎总是估错）：
小型几百到几千行、中型一万上下、大型十万以上。

**★三列对不上的时候，以最后一列为准。**
前两列是**计划**，最后一列是**现状**——**只按现状选档**。
项目要做三个月，不代表第一天就有六个工位在跑。

一句话自检（这条最省事）：

- 你还记得住现在有哪几件事在办 → **小型**
- 记不住了，但还是你一个个亲自派活 → **中型**
- 你已经在跟「管事的」说话、不是跟「干活的」说话 → **大型**

我给建议的格式写死成这样，**人话，不许用术语**：

> 「按你 `项目.md` 里那 8 块模块、第一版做 2 块看，这像**中型**。
>   但你现在只开着两扇窗，所以我建议**先按小型走**，
>   等你真的记不住谁在做什么了再升。**你说了算。**」

---

## 开场：先只做第一件，别一口气装完

**这一档装起来要半天，不可能在一轮里做完。** 读完先做这一件：

1. **先看项目根有没有 `项目.md`**（立项书）。有就先读它。
2. 照第二节第 1 条，给我一份**切位方案**：按模块切，不按工序切。
   列出你建议的位、每位管哪一块、为什么这么切。

**然后停下来等我拍板。**
切位是这一档唯一不能返工的决定——位切错了，后面每一张单都在错的位之间来回转。
我没点头之前别往下装。

★**这份文件不用复述给我。**
★你要是发现我其实还在一个个亲自派活（那是中型的形状），**直接说**，
  让我改用 [2-中型.md](2-中型.md)。这一档一天要花我 1–2 小时，别让我白花。

---

## 一、地图：七份章程，各管一段

| 文件 | 它回答的问题 | 什么时候读 |
|---|---|---|
| [宪法.md](frameworks/大型/宪法.md) | 十章总纲，别的都是它的展开 | 装的第一天 |
| [组织与工位.md](frameworks/大型/组织与工位.md) | 怎么切位、三个特殊角色、模型名册 | 装的第一天 |
| [工单流程.md](frameworks/大型/工单流程.md) | 六种单、状态机、四道必填闸、送达律 | 装的第一天 |
| [任务书三档.md](frameworks/大型/任务书三档.md) | 甲乙丙各写什么、收尾问答 | 派第一张单之前 |
| [三道眼与并线.md](frameworks/大型/三道眼与并线.md) | 判卷、复检、并线次序、上服 | 第一次要并线之前 |
| [记忆与换窗.md](frameworks/大型/记忆与换窗.md) | 接管件、收窗时机、固定工位记忆 | 第一次上下文满之前 |
| [二十条教训.md](frameworks/大型/二十条教训.md) | 本文第四节的原始版本 | 现在 |

---

## 二、装的第一天做四件事

1. **先切位——按模块切，不按工序切。** 这是全部重点。
   按工序切（前端位 / 后端位 / 测试位）的后果是：**一个需求要三个位接力**，
   于是每个需求都在位与位之间排队，而且没人对结果整体负责。
   按模块切（登录位 / 支付位 / 结算位），一个需求一个位从头做到尾。

2. **每位写一份章程。** 一页就够。**★位内的判据必须写进章程才算数**——
   写在交接文件里的规矩，换一个人就没了。

3. **把复检位独立出来。** 从此它不接受任何人的指挥，**包括你**。
   这一条是这一档和中型档的真正分界线：中型档的复检是"另一扇窗"，
   大型档的复检是"另一条指挥链"。

4. **装工单台服务端。** [ticket-desk/](ticket-desk/) 的 `deploy/` 目录。
   十几个位靠单机文件库会撞车。

其余（分发位、三档任务书、模型名册、机器闸）按需要陆续加。

---

## 三、三道眼

一路长上来的形状是这样的，**根上还是那一条**：沟通一扇、干活一扇。

```
小型   策划窗 ──── 工位窗
中型   编排位 ──── 执行位 ──── 复检位
大型   分发位 ──── 总监位 ──── 员工池 ──── 独立复检席
       （策划那一侧也分了工：谁听需求、谁派活、谁判卷）
```

大型档的核心机制是三道眼。一件事从做完到进主线，要过三双**不同**的眼睛：

```
执行方交板  →  ① 判卷（本位总监）：这活做对了吗？
            →  ② 复检（独立于指挥链）：我自己重跑一遍，是不是真的？
            →  ③ 并线（复检位）：能不能进主线？进了会不会撞别人？
```

**★三道眼必须是三个不同的窗。** 任何一道让上一道的人兼任，那一道就等于不存在——
人（和 AI）复检自己的活时，做的是**复述推理**，不是**重新验证**。

判卷的硬闸、并线条件、上服流程见 [三道眼与并线.md](frameworks/大型/三道眼与并线.md)。

---

## 四、最疼的那些教训

每一条后面都有一次真事故。**按「在哪一步会咬你」排，不按重要性排。**

### 做事的时候

**1. 自述全绿不等于跑过。谁执行谁不检。**
执行方复述自己刚才的推理，不是重新验一遍。要另一双眼睛。

**2. ★钉「代码长什么样」的用例，拦不住「代码干了什么」。**
一小时内连着把生产改坏两次，**两次用例都是全绿的**——两次都是人在屏幕上一眼看见的。
`assertIn("某段源码", 文件)` 证明的是「我写下了这行字」，不是「这行字管用」。
**凡是用户会看见的东西，用例必须真的调用那条路。**
源码文本断言不是没用（防止有人删掉某道闸），但它不能是唯一的一道。

**3. ★变异检验活下来的时候，那才是要查的时刻。**
把某处故意改坏、用例却全绿 ≠ 变异没用，而是**这条路今天根本没被测到**。
两次活口两次真问题：一次是用例只测文件后端而线上跑 SQLite；
一次是用例只喂造好的状态、没跑「构造状态」那一步，而 bug 就在构造那一步。
→ **线上用哪个后端，判据就得在哪个后端上再跑一遍。bug 在哪一层，用例就得跑到哪一层。**

**4. 真错别被 `2>/dev/null` 吞掉。**
「这是预期内」的文案必须同时写下**失效条件**——否则它会在真的出事那天继续安慰你。
真实案例：一句很久以前的临时借口（「钥匙未授权前为必然结果」），
钥匙五天前就通了，于是每条失败都被自动贴上「正常」标签，没人去看。

**5. 把东西放进某处，做完必须读回来比一次。**
最坏的一次：自己写的打包脚本用了一个不存在的命令选项，错误被吞掉，
脚本照样打印「已写入」——包里其实什么都没有。

**6. ★只有单向的同步等于没有同步。**
一推一拉必须成对。工具做不了的判断（两边真分叉要不要强推）**就该报错等人**，
不许 `--force` 悄悄抹平。

**7. 本机时区先核，再报别人时钟坏。**
「服务器时间快了 8 小时」有九成概率是你自己时区没核。

**8. 数好得离谱先怀疑量法。**
没有任何改动能解释的提升，判过也要写死「量法可能不同源」。

**9. 先量后改：病根经常和直觉相反。**
「刷新要等十几秒」的直觉答案是「算得慢」，实测是**服务端只花 1.1 秒，
十几秒全花在传 11 MB 未压缩 JSON 上**——第一刀是开压缩，不是加缓存。
**报障描述是症状，不是归因。**

**10. 报数别拿单次采样吹倍数。**
同一个包在同一条线路上实测 1.72～15.41 秒，**波动比改进还大**。
只把「体积少了 69%」当结论，时间说「按同比例下降」。

**11. ★接手现成方案，先量收益能不能兑现。**
照抄别人的方案只降 10.7%，换一个方案降 63.3%。「别人这么做」不是判据。

**12. ★空数组 ≠ 没加载。**
用「是不是数组」判断有没有加载过，而初始化时预填成 `[]` ——
于是一切都被当成「加载过且为空」。**「没加载」必须是 undefined。**

**13. ★同一份数据的两条取法，必须回完全一样的形状。**
整份走一条路（过派生加工），增量走另一条（不过）——于是增量来的数据少了字段，
而**新建的东西必然走增量**。用例要钉**整个键集合**，不要只钉某一个字段。

**14. ★看着「只追加」的东西未必只追加。**
对话线像是只往后加，其实「标已读」会回头改已有行，而**行数一个不变**。
任何按行数切片的缓存都发现不了。→ 会被回头改的东西，别缓存。

**15. ★引代码事实要读实现，不要读注释。**
注释、转发行、过期注释、被截断的输出——四种形态都造成过「取数那步自带缺陷、
却当事实往下写」。表里有一行不等于代码里有机制；测试的名字不等于它的覆盖面。

**16. ★半个东西能骗过全部单元测。**
只注册了一半的实体、只接了一半的线，单元测全绿。**造出来的东西必须真跑一遍。**

### 并线的时候

**17. 零冲突 ≠ 能并。合并树必跑构建 + 全部测试工程。**
一轮撞过五次语义车，其中两次 git 全程沉默。

**18. ★两窗把同一个递增号抬到同值 = 上线即崩。**
协议号、迁移号、版式号。git 只在那一行冲突，随手解就过，
盘上写出「半截 v7」，下一次开机崩。**号面预分要写进任务书。**

**19. 主检出别 `reset --hard`。**
主检出是共用的。跑之前先 `git status` 看别人的未提交件——
实犯过，清掉过别人正在写的文档。

**20. 判过 ≠ 能并。别让主线红着过夜。**
退回要给：隔离归因 + 逐条期望值 + 归属 + 「改测试还是改真源」。

**21. 别人报的分支头多半已过期。**
一天撞过四次。按当前分支头并，但先核祖先关系。

**22. 「台面已合并」≠ 已进主线；「文件进主线」≠ 规矩生效。**

**23. ★核某件事上没上线，要用合并提交去查，不能 grep 单号。**
直接 grep 会命中**后来引用它的那些提交**，结论完全反转。

**24. ★记合并必须逐笔对应刚并的那一笔。**
三张塞一个循环批量记账，结果其中一张根本没并、却被记成已合并。

**25. ★闸不许挂在会移动的东西上。**
「镜像 == 主仓当前头」「相对固定老基点的 diff」「跨仓的活体测试」——
三种形态在定义上就不可能稳定满足。**判据要能自证。**
新鲜度另走一条路，只报不拦。

**26. ★钉哈希的闸必须对着真源的当前头。**
闸绿只说明它自洽。**绿着钉错基准，比没有闸更坏**——
因为你会拿它当证据。

### 判卷与派活的时候

**27. 判语是快照。落笔之前问一句「这条还成立吗」。**
判语常被拍板人在执行窗当面改掉。

**28. 引别人报的事实先自己核，包括上级裁定的前提。**
上级的裁定也建立在某个人报的数上，那个数也可能是抄来的。

**29. ★一堆红里先看谁过了。**
「唯独门槛为 0 的那个过了」——一句话定位出某处硬写了空数组。
全红的时候看谁红没有信息量，看**谁不该过却过了**才有。

**30. ★报绿数必须连基点一起报，而且跑之前先核树干不干净。**
「428 条绿」没有意义，「基点 419 → 完工 428」才有。
在脏树上连报过三个错的红数。落盘型的测试跑两遍数天然不同。

**31. ★「跑两遍凑绿」是假绿。**
有一组用例会写盘、另一组读它，于是次序一变就红。
干净检出连跑两趟，数和名都要复现。

**32. 甲档任务书别写方法。**
弱总监替强执行方定方法是灾难：你会得到一个照做的平庸实现，
而它本来能给你更好的。

**33. ★任务书要站在执行方的权限上写。**
同类错犯过五次：任务书要求做一件执行方根本没权限做的事。
**要求必须同时写进验证脚本**——写不进去的要求，就是执行方做不到的要求。

**34. 跨位现象先定归属再派。**
两位各量一遍会连量三轮，然后互相说对方的数不对。

**35. 一个需求最多核对一次。第二轮直接找拍板人。**
第二轮来回几乎从不收敛，只会把两个人都拖住。

**36. ★写「用户看得见」之前先 grep 消费端。服务端有 ≠ 屏上有。**
同类错连犯过三批。

**37. ★判卷先问「用户实际会走哪条路」。**
场景覆盖也是判据。只走正交直线，让两张单同时漏掉了斜着走那个真因。

### 跟人打交道的时候

**38. 讲数值给拍板人要用白话。**
说「两笔钱」他懂，说「双计费维度」他会说「没听懂」，然后随便写一个。

**39. ★他说「以后可能会改」= 现在就要做成一个具名常量。**
而且要先 grep 一遍有几处消费端，写进任务书。
收尾问答里单列一条：「要改的话改哪个符号」。

**40. ★送得到 ≠ 会被看。**
在对话线里追加一句「必须做的事」，那句话大概率不会被读。
要么改任务书（趁窗还没开），要么要回执点名，要么另开一张单。

**41. ★你在聊天里给的授权，改不了工具层。**
你说「以后这类脚本都可以直接跑」，窗口照样会被它自己的安全机制拦下——
那个机制只看命令长什么样，不看你说过什么；同一支脚本连拦三次第四次过也是有的。
授权要落到**工具认得的地方**，或者把脚本写成它不会拦的形状：
一条 `python x.py` 跑完，脚本自己切目录、自己打回显、末行一句成败结论，
别用 `export …; python …` 这种拼起来的复合命令。
★推论：**聊天里说过、但没写进文件的，等于没说。** 下一扇窗读不到你的聊天记录。

### 附：闸的四条纪律

- **闸只拦「活没做好」。** 卡住执行方的工具账立刻修根，不许拿来当门槛。
- **非业务闸不停车。** 账面类问题记一行、继续做。
- **★文本闸连注释也拦。** 解释「为什么不能写 X」的时候别把 X 抄进注释里——
  一天撞过三次。要改就改措辞，不要改闸。
- **★没被拦住过一次的闸，按不存在算。**
  加完闸要**造一个它该拦住的场景真跑一遍**，看见它红了，再把这次验证写进回执。
  撞过的形状：一道「互相报备再动手」的闸拿关键词扫整份收件箱，
  而收件箱里一句**很久以前**的留言恰好含着那个词，于是等待被直接跳过——
  **闸看着在，其实从建好那天起就没拦住过任何东西**，也没人发现。
  同一条对测试成立：一条**从没红过**的用例，证明不了它在测什么。

### 附：关于「规矩」本身的两条

- **★靠约定成立的规矩，别写进机制。**
  查实「关闭的单 254 张进过、**0 张回头**」，规矩成立；
  但「已办的不再动」不成立——**已答 806 张，回头 214 张**。
  当时范围只要放宽一点点，就会做出一个半年后才被发现的错。
- **★加一道限制之前先查回口。**
  没有回口的闸是**死锁**，不是难度。同一批做掉，或者明说。

---

## 五、怎么跟我配合

同中型档那六条，写进每位的章程。
要改成你自己的：[starter-kit/我的习惯模板.md](starter-kit/我的习惯模板.md)。

大型档另加两条：

7. **只说甲乙丙，别跟我报模型名。** 我要的是「这活多难」，不是「谁在做」。
8. **别跟我报单号。** 报工位，报结论，报还差什么。

---

## 六、降档信号

大型档不是终点。出现下面任意两条，**认真考虑降回中型**：

- 连续两周没有出现过「两扇窗撞同一个文件」；
- 你开始觉得填工单比做事费时间；
- 有超过三分之一的位一周没动过。

**降档比升档难**，因为你会舍不得——毕竟"都建好了"。
具体怎么降：[frameworks/升降档.md](frameworks/升降档.md)。

---

## 最后

**框架是用来省事的，不是用来证明你在正经做事的。**

任何一条规矩，如果你说不出它防的是哪一次具体的事故，那它多半是抄来的。
删掉试试——三天之内没出事，就是真的不需要它。
