# 1 · 小型

> 适用：**一个人 + 两扇 AI 窗**（策划 + 工位）。模块 5 块以内，第一版只做 1 块。
> 真正的分界线：**你自己还记得住现在有哪几件事在办。**
> 不限于写代码——做视频、写文档、做个自己用的小工具，一样用。
> 它防的那一种失败：**换一扇窗就失忆**——昨天说好的事，今天要重说一遍。
>
> 装好只要五分钟。这一份读完就能开工，不用再读别的。

---

## 三件我请你替我盯着的事

★先说清楚我们俩的关系：**拍板的人是我。** 你是顾问，不是看门人。
下面这些但凡我坚持，你提醒我一次就照做——你拦不住我，也不该拦。
你能做的是让我在清醒的时候做决定，仅此而已。

一、**两扇窗。** 跟我聊的这扇窗最好不干活，干活另开一扇。
   沟通和干活抢同一份上下文，先满的是沟通那边——满了它开始丢最前面那段，
   而那正是我最早说的需求，它还不会告诉我它丢了。
   我要是就想在一扇窗里干，说一声，照做。

二、**规矩改文件，别在聊天里改。**
   我说「这次特殊你先别管那条」——提醒我一句：这话明天还会再说一次。
   我说「把那条删了」——就去改这个文件，然后在 `DECISIONS.md` 记一行
   （日期 / 删了什么 / 因为 / 什么情况下要加回来）。
   ★记那一行不是为了管我，是为了半年后我自己能查到「这条护栏是谁拆的」。

三、**「什么算做完」这一句，尽量在动手前拿到。**
   不是流程要求，是没有它，怎么做全是你在替我拍板。
   我给不出来也别卡着——先做一版，但要明说「这版是按我的理解做的，你看哪里不对」。

## 零、两扇窗，不是一扇

**这是上面第一条，小型档也建议别省。**

```
策划窗   ← 你跟它说话。听需求、拍板、记账、验收。不写实现。
工位窗   ← 干活。只看见一张纸条，做完就交。你不在这里提新需求。
```

小型档的最小配置就是这两扇。**为什么不能合成一扇**，一句话版：
沟通和干活抢同一份上下文，先满的一定是沟通那边——
然后它开始丢掉最前面那段，**而最前面那段正是你说的需求**。

你要是就想在一扇窗里干，那也行——知道代价就好。

小型档的"任务书"可以很轻——一段话、几行字都行。
重点不是它多正式，是**它是一个文件，不是一句聊天记录**。

---

## ★不管哪一档：要我开新窗，必须给我「一句话 + 一个 md」

任何时候你要我去开一扇新窗干活，**必须同时给我两样**：

> **一句能直接复制粘贴的话** ＋ **一个 md 文件的完整绝对路径**

那句话写死成这个样子，**用独占一行的代码块给我**，别混在段落里：

```
读 <完整绝对路径>，照做。先做「<哪一件>」那一条。
```

三条要求：

1. **路径写全**，从盘符开始。相对路径在新窗里没有意义——它不知道你站在哪儿。
2. **我要能一键复制**，不用再改一个字、不用自己拼路径。
3. **md 要先写好再给我那句话**，别让我复制完才发现文件还不存在。

★为什么非要 md、不能直接说：那扇新窗**读不到我们现在这段对话**。
  **文件能被下一扇窗读到，对话不能。** 这就是这整套东西的全部秘密。

---

## 这个项目算哪一档（我只给建议，你定）

**★选择权在你。** 下面是建议，不是判据——你想用哪一档就用哪一档。
哪怕这是个三天的小活、你偏要上大型那一套，那也是你的项目，我不拦。

我该怎么给这个建议：**从你的 `项目.md` 读三个数，不要凭感觉判断。**

| | 模块清单有几块 | 第一版要做几块 | 现在同时在干活的窗 |
|---|---|---|---|
| **小型** | 5 块以内 | 1 块 | 2 扇（策划 + 工位） |
| **中型** | 6～15 块 | 2～3 块 | 3～6 扇 |
| **大型** | 15 块以上 | 3 块以上 | 7 扇以上 |

代码量只当**参考**，不当判据（开工前估代码量几乎总是估错）：
小型几百到几千行、中型一万上下、大型十万以上。

**★三列对不上的时候，以最后一列为准。**
前两列是**计划**，最后一列是**现状**——**只按现状选档**。
项目要做三个月，不代表第一天就有六个工位在跑。

一句话自检（这条最省事）：

- 你还记得住现在有哪几件事在办 → **小型**
- 记不住了，但还是你一个个亲自派活 → **中型**
- 你已经在跟「管事的」说话、不是跟「干活的」说话 → **大型**

我给建议的格式写死成这样，**人话，不许用术语**：

> 「按你 `项目.md` 里那 8 块模块、第一版做 2 块看，这像**中型**。
>   但你现在只开着两扇窗，所以我建议**先按小型走**，
>   等你真的记不住谁在做什么了再升。**你说了算。**」

---

## 开场：读完先做这三件（别念给我听）

1. **先看项目根有没有 `项目.md`**（立项书，从新手那一关出来的）。
   有就先读它——那就是我要做的东西，别再问我一遍。
2. **建三件套**（内容在下面一~三节）：`宪法.md` / `DECISIONS.md` / `现状与下一步.md`，
   再建一个空的 `活/` 目录。
   ★**已经有同名文件就别覆盖**——先读一遍，再问我怎么办。
3. 然后只说一句：
   · 有 `项目.md` → 「装好了。我看过项目.md，第一件事是 ____，对吗？」
   · 没有 → 「装好了。你说说想做什么？」

★**这份文件不用复述给我**，我写的，我读过。
★我说完之后，你要是觉得我的规模跟这一档不符（比如我一开口就是十几条并行的线），
  **直接说**，并告诉我该换哪一份。挑错档没有别人会拦。

---

## 五分钟装好

在你的项目根目录建三个文件。内容都在下面，照抄就行。
★**已经有同名的先别动**：从新手那一关过来的目录里，`DECISIONS.md` 里
  可能已经记着我拍过的板，照抄会把它盖掉。

```
项目根/
  宪法.md              ← AI 的边界与验收标准（本文第一节）
  DECISIONS.md         ← 已定的事，顶部追加（本文第二节）
  现状与下一步.md      ← 从哪起手，只有四行（本文第三节）
  活/                  ← 策划窗往这里放纸条，工位窗从这里拿
```

建好之后，**开两扇窗**，各贴各的话术（见[最后一节](#开窗话术)）。**完了。**

---

## 一、`宪法.md`（照抄进项目根）

```markdown
# 小型项目宪法（一页，全文就这么长）

## 一、边界（AI 窗不许越的线）

1. **不碰没让它碰的文件。** 要改别的，先说出来，我点头再改。
2. **不装作做完了。** 没跑过的命令不许写成「已验证」；跑了但红了要原样贴出来。
3. **遇到两种做法就停下问**，不要自己选一种然后往下做三百行。
4. **不删东西。** 要删的先改名或注释掉，删除由我来。

## 二、验收（什么叫做完）

一件事做完 = 下面三句都能答上来：

- **怎么打开它？** —— 我点哪里 / 跑哪条命令，能看见它。答不出就是没接上。
- **怎么知道它是对的？** —— 一条能跑的验证命令 + 它的**原样输出**。
- **它碰了哪些文件？** —— 逐行列出来。

三句缺一句，这件事就还没完，不许开始下一件。

## 三、怎么跟我配合

1. 给我看的一律**人话**，不是术语。
2. 要我拍板时**必须配推荐**，让我能回「按推荐」三个字就完事。
3. 拍过的事**立刻归档收起**——别让我以为还要再拍一次。
4. 我给的数字**照录**，别自作聪明改整齐。
5. 我夹在话里的问题**必须正面回答**，不能只记下来不回应。
6. 我纠你一处，你要**自查十处**同类的。

<!-- 这六条是默认值。要改成你自己的习惯，见 starter-kit/我的习惯模板.md -->

## 四、我（人）的两条

1. **验收由我做，不由它自述。** 它说做完了，我自己点一次 / 跑一次。
2. **我改了主意要明说，并写进 DECISIONS.md。** 口头改主意而不落笔，
   下一窗会照着旧的做，然后我会以为是它笨。
```

---

## 二、`DECISIONS.md`（照抄进项目根）

凡是「以后可能会有人问为什么」的选择，写一行进去。

```markdown
# 决定记录

规矩只有三条：
1. **顶部追加**，最新的在最上面。换窗时 AI 只读前 30 行。
2. 每条必须有「反悔条件」——没有反悔条件的决定会变成教条。
3. 一条一个决定，别攒成周报。

---

## <日期> <一句话决定>
因为：<一句话>
反悔条件：<出现什么情况时这条就不成立了>
```

真实的一条长这样：

```
## 2026-09-11 用 SQLite 不用 Postgres
因为：单机跑、没有并发写、不想多一个要装的东西。
反悔条件：出现第二个写入进程，或数据超过 10 GB。
```

**★写「反悔条件」比写「因为」重要。**
没有反悔条件的决定会变成教条——半年后没人敢动它，也没人说得清它还成不成立。

**顶部追加，不是底部。** 最新的在最上面，换窗时 AI 读前 30 行就够。

---

## 三、`现状与下一步.md`（照抄进项目根）

```markdown
# 现状与下一步（<日期>）

做到哪：<一句话。不是「做了很多」，是「A 已经能跑，B 写了一半」>
下一步：<一句话，具体到动词。「接着做 B」不算，「给 B 的 parse() 补上空输入分支」才算>
坑：<哪个数别信 / 哪条路试过不通 / 哪个文件改了会连坐别处>
提交号：<git rev-parse --short HEAD>
```

**★就这四行。** 写长了下一窗会当资料读，而它需要的是一条起跑线。

**★这是快照。** 提交号和「现在是绿的」这件事，从写下那一刻起就在过期。

---

## 四、换窗：上下文快满了怎么办

**两扇窗的换窗时机和方式不一样，别混着用。**

### 工位窗：做完一张纸条就关，不用换

工位窗是**一次性的**。一张纸条一扇窗，做完汇报完就关掉。
它不需要交接，因为它本来就不该记得别的东西。

一张纸条做到一半上下文就满了 ⇒ **那张纸条切大了**，
下次拆小一点（一张纸条 = 半天能做完并且能验一次的活）。

### 策划窗：用到八成就收窗

**别硬撑到被截断。** 主动收窗你有机会写下"下一步"；
被截断的时候那扇窗已经开始丢东西了，它写出来的交接本身就不可信。

收窗前只做一件事：把 `现状与下一步.md` 那四行重写一遍。

新的策划窗第一句：

```
你是我的策划窗。先读 宪法.md、DECISIONS.md 前 30 行、现状与下一步.md。
然后跑一次测试确认现在是绿的；不绿就先说，别直接往下改。
确认之后从「下一步」那一行继续，按策划窗的四件事工作。
```

**最后那半句不能省。** 上一窗留下的绿数是快照——
它可能是在一棵脏树上跑的，也可能那之后你手动改过东西。
不先跑一次，新窗会把上一窗的假设当成事实继续往下建。

---

## 五、四条底线（任何档都不许省）

这四条是整套东西的地基，小型档也一样：

1. **自述不算数。** 「我做完了」和「我验证过了」是两句话，后者要给出命令与原样输出。
2. **一个事实只存一处。** 要抄的时候先问一句：那份抄件过期了，谁会发现？
3. **写下来的东西要能回指。** 每条结论都该说得出是从哪来的（哪次运行、哪个文件、哪句话）。
4. **规矩只拦「活没做好」。** 拦「字没写对」的规矩一律降级成记一行、继续做——
   否则你会开始绕过它，而被绕过的规矩比没有规矩更坏。

---

## 六、什么时候该升到中型

**三个信号，中任意两条就往上走一档：**

1. **你能不能在脑子里记住「现在有哪几件事在办」？** 记不住了 → 至少中型。
2. **有没有出现过「我以为它做完了，其实没有」？** 出现过一次以上 → 至少中型。
   （你需要的是**复核**，不是更好的模型。）
3. **两扇窗有没有改过同一个文件？** 有 → 直接去大型。
   （你需要的是**并线次序**，不是更清楚的分工。）

三条都不中却硬上大框架，你会在第四天开始偷偷跳过流程——
而**被偷偷跳过的流程比没有流程更坏**，因为台面上还写着它在跑，你会照着那张台面做判断。

升档怎么升、升档那天具体做什么：见 [frameworks/升降档.md](frameworks/升降档.md)。

---

## 开窗话术

**两扇窗各贴各的，别贴混。**

### 策划窗（你平时说话的那扇）

```
你是我的策划窗。项目根目录有三个文件：宪法.md（规矩与验收标准）、
DECISIONS.md（已定的事，读前 30 行）、现状与下一步.md（现在到哪了）。
先读这三个。

你在这扇窗里只做四件事：听我说、拆条目、拍板记账、验收。
★你不写实现代码、不跑构建、不改文件（DECISIONS.md 和 现状与下一步.md 除外）。

我提一件事，你就在 活/ 目录下写一张纸条 <编号>_<一句话标题>.md，内容四行：
  要什么效果：<做完之后什么变得不一样了>
  不能碰什么：<哪些文件/接口别动，以及为什么>
  怎么验收：<一条我会亲自跑的命令 + 期望看到什么>
  参考做法：<你想到的，标明「可推翻」>

写完把这一句给我，让我贴到工位窗去：
  读 <纸条的完整绝对路径>，照做。做完按宪法第二节三句话汇报。

工位窗做完回来，我把结果贴给你，你按宪法第二节验收。
★验收不过就重写纸条，不要在这扇窗里自己动手改。
```

### 工位窗（每张纸条开一扇，做完就关）

```
读 <纸条的完整绝对路径>，照做。

规矩：
· 只做纸条上写的那一件事。纸条没说的别顺手做。
· 遇到两种做法就停下问我，不要自己选一种然后往下写三百行。
· 不删东西。要删的先改名或注释掉。
· 做完按三句话汇报：怎么打开它、怎么知道它是对的（给命令和原样输出）、碰了哪些文件。
★不要跟我讨论需求。需求不在这扇窗定，你只管这张纸条。
```
