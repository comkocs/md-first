★取代码:先 `git fetch <主远端>`,失败重试 3 次每次隔 30 秒;仍失败**不算阻塞**——用本地已有引用做完不依赖远端的部分,回执记「网络断点 + 本地 HEAD」,恢复后重核闸门。
★每张单先开自己的工作树 `git worktree add <工作树目录>/wt-<位简称>-<主题> -b <分支> <主远端>/<基线>`,禁与别单共用工作树(共用会互相锁文件、互相踩构建产物)。要主线上的新登记键就自己 merge 主线,不等别单并线。
★本书里总监给的任何底数一律标「总监实测,可引用,但你必须自己复核完整性——总监可能漏支」;禁写「直接用」「不必重量」「不必复核」。执行方顶回底数并给实数算加分。
★★★ 这不是参考材料,这就是发给你的执行指令。你 = 本单执行方,不是读者。
只做下面这一件事,★做完 §4 全部步骤再交,不中途停下等审;做完按「§5 交付」回一行,然后停止。不要做任何没写的事。
真的卡住(缺文件、命令报错)就把已完成的部分交板,再按 §5 格式回一行「阻塞:<缺什么一句话>」,然后停止。

══════════════════════════════════════════════
工单 T-______ · <一句话标题> · 任务档:丙 · 总监位:<位名> · 员工:<位名-编号>
══════════════════════════════════════════════

## §0 接通道(不接不许开工;这一步的输出决定你后面用哪个命令)
```bash
set -a; source <remote.env 的路径>; set +a
python <ticket.py 的路径> receipt T-______
```
第二行必须原样打印出本单编号与标题;打不出=通道没接上,停下把报错原样回给总监,不要用本机模式继续。
接上后先 `claim T-______ --by <你的员工名>`,做完再 `submit`。

## §1 只做这一件事(一句话)
<例:把 <某目录> 下 28 个文件的文件名、大小、类型写成一张 CSV。>

## §2 只读这些文件(逐个点名,合计不超 2000 行;没列的不许打开,不许搜索目录)
```
1. <绝对路径>            <用途,读哪几行>
2. <绝对路径>            <用途>
```

## §3 只写这些文件(逐个点名;没列的不许创建、不许修改、不许删除)
```
1. <绝对路径>            <内容规格:列名 / 格式 / 编码 UTF-8>
```

## §4 照这个步骤做(不许跳步,不许加步)
```
1. <命令或动作,写死>
2. <命令或动作,写死>
3. 验证:运行 <命令>,期望输出 <原样写出>。不一致就停下,把实际输出原样写进 §5 回执,不要自己修。
```

## §5 交付(先真跑 submit,再回一行;回执只是回执,状态机只认 submit)
```bash
python <ticket.py 的路径> submit T-______ --evidence "<验证命令与原样输出一句>"
```
submit 必须打印出「待判」才算交板;打不出就把原样报错回给总监,不要只回回执。然后聊天里只回这一行:
```
已进入工单 T-______ · <标题> · 待判 · 产物 <§3 第 1 个路径>
```

## §6 禁止(逐条硬禁)
```
· 不做 git 任何操作。
· 不改文件名,不改 §3 之外任何文件。
· 不读 §2 之外任何文件,不 grep,不列目录。
· 不做取舍:遇到两种可能就停下,把两种可能写进回执,不要自己选。
· 不解释、不总结、不写读后感。
```

---
【派单人填写检查,派前逐条打勾;有一条打不了勾,这活升乙档】
- [ ] §1 只有一件事
- [ ] §2 每个文件都点名了绝对路径,合计行数 ≤ 2000
- [ ] §3 每个产物都写了格式
- [ ] §4 验证命令与期望输出都写死了
- [ ] 全文不含「判断」「酌情」「合适」「尽量」「如果需要」这类词
