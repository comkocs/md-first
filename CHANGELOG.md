# 更新记录

版本号只有两截：`1.0` 是第一版，之后的小更新往后排 `1.01`、`1.02`。
每条都写清**改了什么**和**为什么**——「为什么」那半句才是这个仓的正文。

> 版本号与 [md-first-handover](https://github.com/comkocs/md-first-handover) **保持一致**：
> 两仓配套使用，同一个号指的是同一套口径。
> 每一版都打 git tag（`v1.03` 这样），在仓库页的 Releases 里能看到发布历史。

---

## 1.03

这一版分两块：一块来自**上服实践反馈**（一条对 Windows 用户杀伤力极大的 bug），
一块来自**接手实践反馈**（方法层面的两条教训）。

### 修（Windows 上直接不可用）

- **中文输出在非 UTF-8 控制台上全是乱码。**
  Windows 控制台默认按 locale 编码（简中机器是 cp936/GBK），而这个工单台**通篇中文**，
  于是 README 里「30 秒跑起来」那三条命令，在一台没配过环境变量的 Windows 上
  打出来是一片看不懂的字节——**功能全对、退出码是 0、一个字看不懂**。
  不报错也不退非零的坏法最不划算：人只会以为这软件坏了。
  现在 CLI 启动时把 stdout/stderr 强制成 UTF-8（已经是 UTF-8 的一律不碰）。
  ★为什么不写进文档让人自己设 `PYTHONUTF8=1`：得先看得懂那句乱码才知道去查文档；
  而且环境变量传不到被别的脚本、cron、systemd 调起来的地方。
  每个用户都要做一遍的事，就该由程序自己做一次。
  ★这个 bug 藏了很久，因为**测试自己把条件预先设好了**——
  测试帮手里写着 `PYTHONIOENCODING="utf-8"`，子进程一直被喂着这个变量。
  新加的那一组故意不带它、显式设成 gbk，才逼出真实形态。
  **一个把自己要测的条件预先设好的用例，测不到那个条件不成立时的行为。**

### 加（教训）

- **闸的纪律从三条加到四条：没被拦住过一次的闸，按不存在算。**
  加完闸必须造一个它该拦住的场景真跑一遍，看见它红了，再把这次验证写进回执。
  撞过的形状：一道「互相报备再动手」的闸拿关键词扫整份收件箱，
  而收件箱里一句**很久以前**的留言恰好含着那个词，于是等待被直接跳过——
  **闸看着在，其实从建好那天起就没拦住过任何东西**，也没人发现。
  同一条对测试成立：一条从没红过的用例，证明不了它在测什么。
- **新增一条教训：你在聊天里给的授权，改不了工具层。**
  你说「以后这类脚本都可以直接跑」，窗口照样会被它自己的安全机制拦下——
  那个机制只看命令长什么样，不看你说过什么。授权要落到工具认得的地方，
  或者把脚本写成它不会拦的形状（一条命令跑完，脚本自己切目录、自己打回显、
  末行一句成败结论）。★推论：**聊天里说过、但没写进文件的，等于没说。**
- 两处教训清单的标题不再写死条数（`二十条` / `四十条`）——
  写死一个数字的结果是每加一条都要回来改标题，然后总有一次忘了改。

---

## 1.02

同一份上服实践反馈的续报：对方在生产上又跑出两条，都带着补丁。
这两条和 1.01 那批不是一个形状——1.01 修的是「单机跑得通、上服跑不通」那条缝，
这两条修的是**判据齐了却没有出口**，以及**同一个事实在两处各存一份**。

### 修

- **内部单并线并上服之后，没有路把它显式收口。**
  想 `close` 要「实机复验过」；想补 `live` 要一张真登录图，而这类单
  （命令行工具、只读接口）根本没有界面截不出来；`live --shot 免独图` 的旧闸
  只认「**已经**实机复验过、只欠一张独图」的单；而 `close --not-deployed` 会拒绝它，
  理由是「提交号已在部署头里，代码在线上跑着，欠的只是一笔 live 记账」——
  **那句话把真相说得很准，却指向了一条对这类单走不通的路**。
  `awaiting_live_record()` 这个判据早就在了，只是没有一条边让它真的收口。
  现在补上：三条**机器能核的事实**全中时（状态=已合并、标着非用户可感知、
  提交号已在当前值面部署头里），拿那笔上服记录当实机证据直接收口。
  署名也只在这条边上放宽到本位总监——走它时是**核对**不是判断；
  三条判据任一不成立，署名闸当场收回原样（有专门一条反面用例钉这件事）。
  ★需要说清的是：这类单**并线本来就已经是终态**（不进老化告警、不占执行方的手），
  所以这条边解决的是「想显式关掉时没有路」，不是「单子卡住没人管」。
- **位名落进 `slots.json` 之后再不与 config 校正，而且是静默的。**
  另一半代码（argparse 的 `choices`、`say`/`transfer`/`create` 的判据）全程直读
  `config.SLOTS`，两边一旦不一致：网页按 `slots.json` 渲染的「转给指定总监」下拉，
  选谁服务端都当场拒收；更难查的是「要你去唤醒的窗口」——它也按 `slots.json`
  逐位查未读，于是**真实位的对话线压根没被遍历**，`say` 写进去多少条都不冒出来，
  连一个字的报错都没有。触发它不需要谁做错事：装机脚本
  「先起服务、后写 `TICKET_CONFIG`」就够了。
  现在只校正**位名成员**：config 里有的补进来，config 里没有的**停用而不是删除**
  （历史单的「所属总监位」还引用着它们）。**手改项一个字不动**——
  主力模型集合、模型名册、停用阈值仍然不回写，那正是当初不校正的理由，
  而那个理由对它们完全成立。位名跟着 config 走，不等于整份名册跟着 config 走。

### 文档

- `deploy/上服清单.md` 明写 **`update.sh` 永远跑新包里的那一份**，不是
  `<安装目录>/app/deploy/` 里那份。后者是上一次上服装进去的旧脚本，
  这次包里带的新参数它不认识，会被自己的参数闸拦下——而且**每次新增参数都会撞一次**，
  `--port` 与 `--target` 就是 1.01 新加的。判据一句话：
  `--source` 指哪个目录，`update.sh` 就跑哪个目录里的那一份。

---

## 1.01

这一版全部来自一份**上服实践反馈**：有人把 `ticket-desk/` 装到自己的服务器上，
照 `deploy/上服清单.md` 真装了一遍，把撞到的八条问题连同两个补丁一起寄了回来。
八条里有四条是同一个形状——**「单机跑得通」与「上服跑得通」之间的那条缝**：
打包少打一个文件、install 少拷一个目录、配置放在会被上服删掉的位置。
这一版把那条缝本身也加了一道闸。

### 修（阻断）

- **上服包不打 `README.md`，导致上服闸②每次必拦。**
  `tests` 里有两条文档一致性用例直接读 `README.md`，它不在包里不是跳过，
  是 `FileNotFoundError` 直接红——于是**照文档操作的新使用方，第一次上服就被自己的闸拦死**，
  而报错长得像「你的测试坏了」，不像「包少打了一个文件」。
  改法是**补包**，不是给用例加跳过：跳过等于在上服闸里永久关掉两道文档闸。
  `pack.sh` 的脏树闸也一并覆盖到 `README.md` 与 `tests`。

### 改（口径）

- **对话线放开「总监之间互相说一句」。**
  原口径是「只收拍板人、本位总监、总编排三方」，本意是别让别位串门；
  但它把传话这件事本身也堵死了——一句「我这边上完服了，你那边可以开窗」
  只剩下建一张疑问单这条路，而疑问单是**要人答的**、会进对方待答队列。
  现在收「拍板人 + 任何在册位名」。串门的顾虑由留痕兜着：每一行都写死发言人。
  **员工那条窄缝一个字没放松**（仍须带 `--ref` 且那张单指派给他本人）。
  `say` 与 `say_uploaded` 原来各拼了一份同样的判据，这次抽成一处共用。

### 修（上服链）

- `install.sh` 现在会把 `deploy/` 一起拷进 `app/`，`update.sh` 也跟着刷新它。
  此前文档里那条 cron 指向 `app/deploy/backup.sh`，而那个文件根本不存在——
  静默失败，cron 不报错，等到要恢复那天才知道一次备份都没有。
- `install.sh` 新增 `--desk-config`：把名册装到 `<安装目录>/config.json`（`app/` **之外**）
  并自动写 systemd drop-in 指过去。此前按文档把配置放进包目录的人，
  **第一次上服就丢名册**，而且不报错、是静默回落到示例名册。
- `install.sh` 新增 `--python-bin`，不再写死 `/usr/bin/python3`。
  有些机器上往系统 python 装包这件事本身是不被允许的，而上服闸①要求
  跑服务的那个 python 能 `import PIL`；此前那种环境只剩「手改单元文件」一条路，
  而单元文件正是 `install.sh` 下次会覆盖的东西。
- `install.sh` 新增 `--no-firewall`，并且执行前明说它会改 ufw。
  改防火墙是一次**全机**写入，在「只授权你动这个目录」的环境里属于越界。
- `update.sh` 的端口改为**从 systemd unit 的 `--port` 现读**（读不到才回落 `8443`），
  也可用 `--port` 明给。此前端口不是 8443 又忘了传时，代码其实已经替换、服务也已经起来，
  后置闸却在 8443 上白等 10 秒然后报失败——**假红会诱导人去做多余的动作**。
  （同一条路脚本里读 `--token-file` 时已经走过一遍。）
- `update.sh` 新增 `--target`，不再把上服记录写死在 `production`。
  此前工单台给自己上服，会把「production 部署头」那一格覆盖成工单台自己的提交号，
  而在拿它管别的系统的地方，那一格记的是**被管理的业务系统**的头。

### 修（话不对）

- `staff fix --memory` 在远程模式下不再恒定误报「父目录还不在」。
  服务端拿着客户端给的路径去查**自己的**文件系统，结论永远是「不存在」。
  现在判的是路径的**形状**（它写的是不是另一台机器的盘），
  不需要谁把「我是远程」这个标志一路传进来——标志会漏传，形状不会。
- `--memory` 的帮助示例改成正斜杠。此前走 `str(Path)`，在 Windows 上产出反斜杠，
  照抄进 bash 会被转义吃掉。与 `channel.forward_slashes` 同一条口径。
- `new` 的必填闸改为**一次列全**。派单有五项必填、分在两层闸上，
  此前撞一条补一条，一张单要重敲三四遍长命令行，而每重敲一遍都是一次手抖的机会。
  每条的话一字未改，变的只是「一次说几条」。

### 加（闸）

- 新增一组**打包回环**用例：真打一次包 → 解出来 → 在**解包目录**里跑那两条文档用例。
  这一组堵的就是上面那条缝——它在仓树上永远看不见，只有真打一次包才露头。
  它打的是**当前这棵树**（捧进一个临时 git 仓，真仓一个字节不碰），
  所以改了 `pack.sh` 当场就能看见效果，而不是等提交完才知道打漏了文件。

### 文档

- `README.md` 把测试成绩按「在哪跑」分成三种列出来。
  给自己配过 `ticket_desk/config.json` 的人一跑测试就会看到一条 skip，
  那是有意的，此前最容易被误判成回归。
- `README.md` 补了 `{ticket}` 占位符配合 `--taskbook-unchecked` 的完整三步示例。
- `deploy/上服清单.md` 写清了新增的五个参数，以及「名册配置不要放进包目录」。

---

## 1.0

第一个公开版本。四份入口文档（新手 / 小型 / 中型 / 大型）、七份章程、
starter-kit，以及工单台 `ticket-desk/`。Apache-2.0。
